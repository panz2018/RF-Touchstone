{
  "name": "touchstone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "run-p --print-label test:unit test:coverage build",
    "test:vitest": "vitest",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "tsc && vite build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@vitest/coverage-v8": "^3.0.5",
    "happy-dom": "^17.0.2",
    "npm-run-all": "^4.1.5",
    "typescript": "~5.7.2",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  }
}
